# repoflare-core

Shared repository-intelligence engine for RepoFlare — scanning, parsing, the knowledge
graph, and (later) change/impact analysis and AI-assisted reasoning. Used by both the CLI
and the VS Code extension. See `../docs/ARCHITECTURE.md`.
