Bob IDE task session summary screenshots (and exported task history, if you want the extra
detail) go here — required by the official IBM Bob 2.0 hackathon submission form: the repo
must include "each team member's IBM Bob task session summary screenshots," alongside the
actual code/files Bob assisted with elsewhere in the repo. See `AGENTS.md` for what's
already built and the "Next up" list of tasks to run through Bob IDE.

Naming suggestion per task: `<your-name>-<task-slug>-<n>.png` for the screenshot,
`<your-name>-<task-slug>-<n>.md` for the exported history, so multiple team members'
sessions don't collide.
